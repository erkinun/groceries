import { extend } from './utils';
import base from './base';

export default extend(base, {
  backgroundPrimary: '#7b7b7b',
  contrast: '#444444',
  backgroundSecondary: '#444444',
  textPrimary: '#fff',
});
